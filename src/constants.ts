/** indicates the highest level of quality that an item can be */
export const MAX_QUALITY_VALUE = 25;
export const MIN_SELL_IN_VALUE = -5;
/** indicates the amount of days remaining before a food item goes bad where it is no longer fresh, and thus degrades at twice the speed */
export const FRESHNESS_TURNING_POINT = 5;
